info:
  name: postpackage
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:5000/package
  body:
    type: json
    data: |-
      {
        "description":"needs to stay cool",
        "weight":"5"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
